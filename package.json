{
  "name": "keys-stocklana",
  "private": true,
  "version": "0.1.0",
  "description": "KEYS — progressive financial stewardship proof runtime.",
  "scripts": {
    "test": "node --test test/*.test.mjs && node --check scripts/dev-api.mjs",
    "test:anchor": "mocha --timeout 120000 tests/anchor-authority-proof.cjs",
    "demo": "node scripts/demo-run.mjs",
    "api": "node scripts/dev-api.mjs"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.32.1"
  },
  "devDependencies": {
    "mocha": "11.7.2"
  }
}
