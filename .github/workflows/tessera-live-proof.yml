name: tessera-live-proof

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/tessera-adapter.mjs'
      - 'scripts/tessera-live-proof.mjs'
      - 'test/tessera-adapter.test.mjs'
      - '.github/workflows/tessera-live-proof.yml'
      - 'package.json'

permissions:
  contents: read

jobs:
  live-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Verify live Tessera integration
        run: npm run proof:tessera
